import { motion } from 'framer-motion';

export const NavigationUnderlineComponent: React.FunctionComponent = () => (
  <motion.div layoutId='navigation-underline' className='navigation-underline' animate />
);
